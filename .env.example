# Injected into pages/index.html by scripts/serve-local.js and the Pages workflow.
# Do not commit real values. For GitHub Pages, add the same names as repository secrets.
GOOGLE_MAPS_API_KEY=
CUSTOM_MAP_ID=
CUSTOM_MAP_NAME=